/**
 * @file Defines the possible states for a student's attendance record.
 */

/**
 * Enumeration representing attendance status for a student.
 */
export enum AttendanceStatus {
  /** The student attended the class. */
  PRESENT = "presente",

  /** The student did not attend the class. */
  ABSENT = "ausente",
}
